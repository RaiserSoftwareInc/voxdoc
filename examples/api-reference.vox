{
  "$schema": "https://voxformat.dev/schema/v1.json",
  "meta": {
    "id": "b7e3a1d9-4c82-4f1a-9e6d-8a2b5c7d0e3f",
    "title": "Vox API Reference",
    "description": "API documentation for the Vox document management service, covering authentication, document CRUD operations, and compilation endpoints.",
    "version": "1.0.0",
    "authors": ["Vox Team"],
    "tags": ["api", "reference", "technical"],
    "status": "published",
    "created": "2026-04-12T00:00:00.000Z",
    "updated": "2026-04-12T00:00:00.000Z",
    "variables": {
      "api_version": "v1",
      "base_url": "https://api.voxformat.dev"
    },
    "provenance": {
      "generated_by": "vox-cli",
      "reviewed_by": "human",
      "approved_blocks": [],
      "flagged_blocks": []
    },
    "accessibility": {
      "language": "en",
      "reading_level": "technical"
    }
  },
  "blocks": [
    {
      "id": "api-title",
      "type": "heading",
      "level": 1,
      "content": "Vox API Reference"
    },
    {
      "id": "api-intro",
      "type": "paragraph",
      "content": "The {{base_url}}/{{api_version}} API provides programmatic access to create, validate, compile, and manage Vox documents. All endpoints accept and return JSON. This reference covers authentication, available endpoints, and code examples in multiple languages."
    },
    {
      "id": "auth-heading",
      "type": "heading",
      "level": 2,
      "content": "Authentication"
    },
    {
      "id": "auth-paragraph",
      "type": "paragraph",
      "content": "All API requests must include a Bearer token in the `Authorization` header. Tokens are issued through the developer dashboard and expire after 90 days. Each token is scoped to a specific organization and set of permissions."
    },
    {
      "id": "auth-warning",
      "type": "callout",
      "variant": "warning",
      "title": "Keep Your API Keys Secure",
      "content": "Never expose your API token in client-side code, public repositories, or logs. Use environment variables or a secrets manager to store tokens. If a token is compromised, revoke it immediately from the developer dashboard."
    },
    {
      "id": "auth-code",
      "type": "code",
      "language": "bash",
      "content": "# Set your API token as an environment variable\nexport VOX_API_TOKEN=\"your-token-here\"\n\n# Make an authenticated request\ncurl -H \"Authorization: Bearer $VOX_API_TOKEN\" \\\n     {{base_url}}/{{api_version}}/documents",
      "filename": "auth-example.sh"
    },
    {
      "id": "endpoints-heading",
      "type": "heading",
      "level": 2,
      "content": "Endpoints"
    },
    {
      "id": "endpoints-intro",
      "type": "paragraph",
      "content": "The following table lists all available API endpoints. All paths are relative to `{{base_url}}/{{api_version}}`."
    },
    {
      "id": "endpoints-table",
      "type": "table",
      "headers": ["Method", "Path", "Description", "Auth"],
      "rows": [
        ["POST", "/documents", "Create a new Vox document", "Required"],
        ["GET", "/documents", "List all documents in the organization", "Required"],
        ["GET", "/documents/:id", "Retrieve a specific document by ID", "Required"],
        ["PUT", "/documents/:id", "Update an existing document", "Required"],
        ["DELETE", "/documents/:id", "Delete a document permanently", "Required"],
        ["POST", "/documents/:id/validate", "Validate a document against the schema", "Required"],
        ["POST", "/documents/:id/compile", "Compile a document to HTML", "Required"],
        ["GET", "/documents/:id/comments", "List review comments on a document", "Required"]
      ],
      "caption": "Available API endpoints",
      "summary": "A table of all Vox API endpoints showing the HTTP method, URL path, description of what each endpoint does, and whether authentication is required."
    },
    {
      "id": "create-heading",
      "type": "heading",
      "level": 3,
      "content": "Create a Document"
    },
    {
      "id": "create-paragraph",
      "type": "paragraph",
      "content": "Send a `POST` request to `/documents` with a JSON body containing the document metadata and initial blocks. The server assigns an ID and returns the created document."
    },
    {
      "id": "create-examples",
      "type": "tabs",
      "panels": [
        {
          "label": "JavaScript",
          "language": "javascript",
          "content": "const response = await fetch('{{base_url}}/{{api_version}}/documents', {\n  method: 'POST',\n  headers: {\n    'Authorization': `Bearer ${process.env.VOX_API_TOKEN}`,\n    'Content-Type': 'application/json',\n  },\n  body: JSON.stringify({\n    title: 'My Document',\n    description: 'A new Vox document',\n    blocks: [],\n  }),\n});\n\nconst doc = await response.json();\nconsole.log('Created document:', doc.meta.id);"
        },
        {
          "label": "Python",
          "language": "python",
          "content": "import os\nimport requests\n\nresponse = requests.post(\n    '{{base_url}}/{{api_version}}/documents',\n    headers={\n        'Authorization': f'Bearer {os.environ[\"VOX_API_TOKEN\"]}',\n        'Content-Type': 'application/json',\n    },\n    json={\n        'title': 'My Document',\n        'description': 'A new Vox document',\n        'blocks': [],\n    },\n)\n\ndoc = response.json()\nprint(f'Created document: {doc[\"meta\"][\"id\"]}')"
        },
        {
          "label": "curl",
          "language": "bash",
          "content": "curl -X POST {{base_url}}/{{api_version}}/documents \\\n  -H \"Authorization: Bearer $VOX_API_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"title\": \"My Document\",\n    \"description\": \"A new Vox document\",\n    \"blocks\": []\n  }'"
        }
      ]
    },
    {
      "id": "compile-heading",
      "type": "heading",
      "level": 3,
      "content": "Compile a Document"
    },
    {
      "id": "compile-paragraph",
      "type": "paragraph",
      "content": "The compile endpoint validates the document, checks accessibility requirements, resolves variables, and generates self-contained HTML. The response includes the compiled HTML and any warnings encountered during compilation."
    },
    {
      "id": "compile-examples",
      "type": "tabs",
      "panels": [
        {
          "label": "JavaScript",
          "language": "javascript",
          "content": "const response = await fetch(\n  '{{base_url}}/{{api_version}}/documents/doc-id/compile',\n  {\n    method: 'POST',\n    headers: {\n      'Authorization': `Bearer ${process.env.VOX_API_TOKEN}`,\n    },\n  }\n);\n\nconst result = await response.json();\nif (result.success) {\n  // Write the HTML to a file\n  fs.writeFileSync('output.html', result.html);\n}"
        },
        {
          "label": "Python",
          "language": "python",
          "content": "import os\nimport requests\n\nresponse = requests.post(\n    '{{base_url}}/{{api_version}}/documents/doc-id/compile',\n    headers={\n        'Authorization': f'Bearer {os.environ[\"VOX_API_TOKEN\"]}',\n    },\n)\n\nresult = response.json()\nif result['success']:\n    with open('output.html', 'w') as f:\n        f.write(result['html'])"
        },
        {
          "label": "curl",
          "language": "bash",
          "content": "curl -X POST {{base_url}}/{{api_version}}/documents/doc-id/compile \\\n  -H \"Authorization: Bearer $VOX_API_TOKEN\" \\\n  -o output.html"
        }
      ]
    },
    {
      "id": "getting-started-heading",
      "type": "heading",
      "level": 2,
      "content": "Getting Started"
    },
    {
      "id": "getting-started-steps",
      "type": "steps",
      "steps": [
        {
          "title": "Create an account and get your API token",
          "blocks": [
            {
              "id": "step1-content",
              "type": "paragraph",
              "content": "Sign up at the Vox developer dashboard and navigate to **Settings > API Tokens**. Click **Generate Token**, give it a descriptive name, and copy the token value. Store it securely as an environment variable."
            }
          ]
        },
        {
          "title": "Install the SDK for your language",
          "blocks": [
            {
              "id": "step2-content",
              "type": "code",
              "language": "bash",
              "content": "# JavaScript / TypeScript\nnpm install @vox/sdk\n\n# Python\npip install vox-sdk"
            }
          ]
        },
        {
          "title": "Create your first document via the API",
          "blocks": [
            {
              "id": "step3-content",
              "type": "paragraph",
              "content": "Use the `POST /documents` endpoint to create a new document. Add blocks using `PUT /documents/:id` and then compile the result using `POST /documents/:id/compile`."
            }
          ]
        },
        {
          "title": "Validate and compile",
          "blocks": [
            {
              "id": "step4-content",
              "type": "paragraph",
              "content": "Before publishing, always validate your document with `POST /documents/:id/validate` to ensure all accessibility requirements are met. Then compile to HTML and verify the output renders correctly."
            }
          ]
        }
      ]
    },
    {
      "id": "var-api-version",
      "type": "variable_def",
      "key": "api_version",
      "value": "v1"
    },
    {
      "id": "var-base-url",
      "type": "variable_def",
      "key": "base_url",
      "value": "https://api.voxformat.dev"
    }
  ],
  "comments": []
}
